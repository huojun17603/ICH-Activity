开发日志
目的：做一个活动的通用模板
大致思路如下：
1.帮助触发，但并不参与逻辑，而是执行逻辑
2.在奖励机制中存在一套固有逻辑，但也可以使用自定义的奖励逻辑
3.所谓的奖品全都是系统中已经存在的一种资源；实物除外（工作人员主动联系）
[2018-01-12]:
注意：如果活动是像抢什么小米之类的为了更好的控制数量上的问题请采用排队机制
排队机制：用户请求后顺序保留用户请求（提示用户等待，并循环请求结果），待系统批量处理后（快速），返回用户信息
